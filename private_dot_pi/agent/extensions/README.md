# Pi extensions

Local extensions live here. Referenced from `~/.pi/agent/settings.json` via
`"extensions": ["extensions"]`. See `docs/extensions.md` in the
`@earendil-works/pi-coding-agent` package for the extension API.

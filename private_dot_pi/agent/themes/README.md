# Pi themes

Custom themes live here. Referenced from `~/.pi/agent/settings.json` via
`"themes": ["themes"]`. Select one by setting `"theme": "<name>"`. See
`docs/themes.md` in the `@earendil-works/pi-coding-agent` package.

# Pi prompt templates

Custom `/prompt:name` templates live here. Referenced from
`~/.pi/agent/settings.json` via `"prompts": ["prompts"]`. See
`docs/prompt-templates.md` in the `@earendil-works/pi-coding-agent` package.
